import base64

# 아이디와 비밀번호 입력
user = "ML01"
password = "Aielal!"

# "아이디:비밀번호" 문자열 생성
raw = f"{user}:{password}"

# Base64 인코딩
encoded = base64.b64encode(raw.encode("utf-8")).decode("utf-8")

# Authorization 헤더에 붙일 문자열
auth_header = f"Basic {encoded}"

print(auth_header)

